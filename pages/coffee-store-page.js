import React from "react";

/**
 * Discover-coffee-stores - version 1.01 -  coffee-store-page
 * - Fetaures:
 * 
 *    --> This is an example of nested route ( Buolding CoffeeStore Page )
 * 
 * Note: conventions: 
 * 
 *      - have to be in minus case the file.
 * 
 *      - located in pages directory. 
 * 
 *      - and if it has an extended name separation with dash  ' - '
 *        is an advice ( for readbility )
 * 
 *      - exported as default  
 */

const CoffeStore = () => {

    return(
        <div> Coffe Store Page </div>
    )
}

export default CoffeStore;